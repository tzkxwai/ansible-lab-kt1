{
  "name": "Ansible lab (KT1)",
  "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ansible sshpass openssh-client python3-passlib && ansible-galaxy collection install -r collections/requirements.yml",

  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.ansible"
      ]
    }
  }
}
